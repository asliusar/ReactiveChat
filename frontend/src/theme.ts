import createMuiTheme, {ThemeOptions} from '@material-ui/core/styles/createMuiTheme';
import {PaletteOptions, SimplePaletteColorOptions, TypeBackground} from '@material-ui/core/styles/createPalette';
import {ZIndexOptions} from '@material-ui/core/styles/zIndex';

const THEME = {
    
};

export const CUSTOM_THEME = createMuiTheme(THEME);
